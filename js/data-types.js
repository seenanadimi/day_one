// Primitive Data Structures for JS
// boolean, strings, numbers are the main three data types being used.... most of the time

const no = false; // true or false value; boolean value
let abc = "abc"; // strings (value of words)
export var count = 0; // numbers/integers: (int, float)
// const is always constant/forever. let will allow you to later change a variables value. var is the least strict of the three variable types (least used).

// Container Compound Data Structures

const allTheDataTypes = [no, count, abc]; // Array (list)
const object = {
  //OBJECT
  no: false,
  count: 0,
  abc: "abc",
  name: "Seena",
  age: 28,
  isStudent: true,
};

